<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Research Idea Site | 研究支援システム</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Outfit:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #0f172a; color: #f8fafc; }
        h1, h2, h3 { font-family: 'Outfit', sans-serif; }
        .glass { background: rgba(30, 41, 59, 0.7); backdrop-filter: blur(12px); border: 1px solid rgba(255, 255, 255, 0.1); }
        .gradient-text { background: linear-gradient(135deg, #38bdf8, #818cf8); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
    </style>
</head>
<body class="min-h-screen flex flex-col">
    <nav class="glass sticky top-0 z-50 px-6 py-4 flex justify-between items-center mb-8">
        <a href="/" class="text-2xl font-bold gradient-text">Research Idea Site</a>
        <div class="space-x-6">
            <a href="/" class="hover:text-sky-400 transition">ダッシュボード</a>
            <a href="/new" class="hover:text-sky-400 transition">新規アイデア</a>
            <a href="/search" class="hover:text-sky-400 transition">検索</a>
        </div>
    </nav>

    <main class="flex-grow container mx-auto px-6 max-w-5xl">
        <% if session[:message] %>
            <div class="bg-sky-900/50 border border-sky-500 text-sky-200 px-4 py-3 rounded mb-6">
                <%= session.delete(:message) %>
            </div>
        <% end %>

        <%= yield %>
    </main>

    <footer class="py-8 text-center text-slate-500 text-sm border-t border-slate-800 mt-12">
        &copy; 2025 Research Idea Site - HCI & Media Art Research Support
    </footer>
</body>
</html>
